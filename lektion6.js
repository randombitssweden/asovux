/*
var pre = 0;
var post = 1;
document.body.innerHTML+="Let's go fibinatchi!"
for (var i = 0; i < 10 ; i++) {
    var sum = pre + post; 
    document.body.innerHTML+=sum+" ";
    pre = post; 
    post = sum; 
    if (i == 9) {
        document.body.innerHTML+=i+1;
    } else {
        document.body.innerHTML+=i+1+",";
    }
}*/

//while ()